<nav class="navbar bg-light border-bottom py-2 px-4">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <!-- Brand Image -->
    <a class="navbar-brand d-flex align-items-center" href="#">
      <img src="https://www.google.com/images/branding/googlelogo/2x/googlelogo_color_272x92dp.png" width="100" height="30" class="d-inline-block align-top" alt="Google Logo">
    </a>

    <!-- Navbar Items -->
    <div class="d-flex flex-row gap-4">
      <ul class="navbar-nav d-flex flex-row align-items-center gap-5">
        <li class="nav-item">
          <% if client_signed_in? %>
            <%= link_to 'Home', client_root_path, class: "nav-link text-dark fw-bold" %>
          <% else %>
            <%= link_to 'Sign in', new_client_session_path, class: "nav-link text-dark fw-bold" %>
          <% end %>
        </li>
        <li class="nav-item">
          <a class="nav-link text-dark" href="#">Coins</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-dark" href="#">Claim</a>
        </li>

        <!-- Language Dropdown -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-dark" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Language
          </a>

          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
            <li>
              <%= link_to 'English',params.permit!.merge(locale: 'en'), class: 'dropdown-item' %>
            </li>
            <li>
              <%= link_to 'Tagalog', params.permit!.merge(locale: 'tg'), class: 'dropdown-item' %>
            </li>
          </ul>
        </li>
      </ul>

      <!-- Navbar Toggler (Menu Button) always visible -->
      <% if client_signed_in? %>
        <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
      <% end %>
    </div>
  </div>
</nav>

<!-- Offcanvas Navbar (for small screens) -->
<%= render 'client/shared/menu' %>
